{
  "name": "fintech-core-platform",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "dev:accounts": "pnpm --filter @fintech/accounts-service start:dev"
  },
  "engines": {
    "node": ">=20"
  }
}
